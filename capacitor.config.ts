import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'expenses-manager',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
